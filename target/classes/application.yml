logging:
  level:
    root: ERROR
spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/Trivia?createDatabaseIfNotExist=true
    username: postgres
    password: pass@word1
    driverClassName: org.postgresql.Driver

